type FormErrosType = {
  MAX_LENGTH: string;
  MIN_LENGTH: string;
  REQUIRED: string;
  MAX: string;
  MIN: string;
  EMAIL: string;
};

export const formErrorsTypes: FormErrosType = {
  MAX_LENGTH: "maxlength",
  MIN_LENGTH: "minlength",
  REQUIRED: "required",
  MAX: "max",
  MIN: "min",
  EMAIL: "email",
};
